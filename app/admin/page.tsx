// Placeholder — the real admin dashboard shell is built in BUILD_PLAN.md
// Phase 7. This page exists in Phase 2 only to verify the proxy guard.
export default function AdminPlaceholderPage() {
  return (
    <div className="p-8">
      <h1 className="text-2xl font-semibold">Admin</h1>
      <p className="text-muted-foreground">Dashboard shell arrives in Phase 7.</p>
    </div>
  );
}
